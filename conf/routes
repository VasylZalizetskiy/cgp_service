# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET           /golden/:value                                                @controllers.Application.toGoldenProportionCode(value: Double)

GET           /decimal/:value                                               @controllers.Application.toDecimal(value: String)